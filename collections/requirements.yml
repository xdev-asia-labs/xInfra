---
# Ansible Galaxy requirements
# Install: ansible-galaxy install -r collections/requirements.yml

collections:
  - name: ansible.posix
    version: ">=1.5.0"
  - name: community.general
    version: ">=8.0.0"
  - name: community.postgresql
    version: ">=3.0.0"
  - name: ansible.netcommon
    version: ">=5.0.0"
